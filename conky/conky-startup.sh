#!/bin/sh

if [ "$DESKTOP_SESSION" = "/usr/share/xsessions/plasma5" ]; then 
   # No widgets enabled!
   exit 0
fi
if [ "$DESKTOP_SESSION" = "/usr/share/xsessions/plasma5" ]; then 
   # No widgets enabled!
   exit 0
fi
